{
  "name": "academic-analytics-monorepo",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend-nextjs",
    "start": "npm run start --prefix frontend-nextjs"
  }
}
